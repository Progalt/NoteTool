#pragma once
#include "Widget.h"

namespace gui
{
	// Dock is a list of buttons down the side of the screen

	class Dock : public Widget
	{
	public:

		void GenerateVertexList(DrawList& drawList) override { }

	private:
	};
}